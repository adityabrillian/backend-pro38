<!-- Modal -->
<div
  class="modal fade"
  id="editModal"
  tabindex="-1"
  role="dialog"
  aria-labelledby="editModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="editModalLabel">Update Bank</h5>
        <button
          type="button"
          class="close"
          data-dismiss="modal"
          aria-label="Close"
        >
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <form
        action="/admin/bank?_method=PUT"
        method="POST"
        enctype="multipart/form-data"
      >
        <div class="modal-body">
          <div class="form-group">
            <label for="name" class="col-form-label">Nama Bank</label>
            <input
              required
              name="nameBank"
              type="text"
              class="form-control nameBank"
              placeholder="Isi nama bank"
            />
          </div>
          <div class="form-group">
            <label for="name" class="col-form-label"
              >Nomor Rekening</label
            >
            <input
              required
              name="nomorRekening"
              type="text"
              class="form-control nomorRekening"
              placeholder="Isi nomor rekening"
            />
          </div>
          <div class="form-group">
            <label for="name" class="col-form-label">Nama Pemilik</label>
            <input
              required
              name="name"
              type="text"
              class="form-control name"
              placeholder="Isi atas nama pemilik rekening"
            />
          </div>
          <div class="form-group">
            <label for="name" class="col-form-label image">Image</label>
            <input name="image" type="file" class="form-control" />
            <span>Pilih gambar untuk update image</span>
          </div>
        </div>
        <div class="modal-footer">
          <input type="hidden" class="id" name="id" />
          <button type="button" class="btn btn-secondary" data-dismiss="modal">
            Tutup
          </button>
          <button type="submit" class="btn btn-primary">Update</button>
        </div>
      </form>
    </div>
  </div>
</div>
